<%= @user_skill.title %>

<%= form_for @user_skill do |f|                %>
  <%= f.label  :progress %> <p>explain your experience with this course so far</p>
  <%= f.text_area :progress, rows: 10 %>

<br/><br/>

  <%= f.submit @user_skill.new_record? ? "Add to my library" : "Update Skill", class: 'btn btn-primary'    %>
<% end %>